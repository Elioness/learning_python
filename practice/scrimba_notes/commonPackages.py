# Randomness
# import random, and google methods that are included in this package

# Time It and Performance
# - checks how long it takes to be run a code
# - $ import timeit
# - $ print(timeit.timeit('test_function()', globals=globals(), number=10))
#       - inside the method, place the function, copy globals=globals(), numbers is how many times you want the function to run
